# -----------------------------------------------------------------------------
# Example Docker environment file (safe, generic placeholders)
# Copy to: .env.docker
# -----------------------------------------------------------------------------

# Base image versions
ALPINE_VERSION="3.21"

# Rescale/Besu node Docker image
RESCALE_NODE_IMAGE="registry.example.com/your-org/besu:YYYY-MM-DD"

# Besu node network settings (Docker bridge network IP)
BESU_NODE_IP="172.29.0.2"
BESU_NODE_PORT="8545"

# Local paths (adjust for your OS)
# Windows example: "C:/Users/<you>/path/to/LedgerAdapter"
# Linux/macOS example: "/home/<you>/path/to/LedgerAdapter"
MODULE_ROOT_FLD="/path/to/LedgerAdapter"

# Genesis contracts and CA certificate paths
GENESIS_CONTRACTS_PATH="/path/to/LedgerInfrastructure/build/rescalenode/genesis-contracts.json"
CA_CERTIFICATE_PATH="/path/to/LedgerInfrastructure/build/rescalenode/ca/cert.pem"

# Docker compose files
COMPOSE_BUILD_FILE="/path/to/LedgerAdapter/docker/docker-compose.test.yml"
COMPOSE_TEST_FILE="/path/to/LedgerAdapter/docker/docker-compose.test.yml"

# Node authentication credentials
USERNAME_ROOT="root"
PASSWORD_ROOT="changeMeRootPassword"

USERNAME_ETH="eth"
PASSWORD_ETH="changeMeEthPassword"

USERNAME_PUBLIC="public"
PASSWORD_PUBLIC="changeMePublicPassword"
